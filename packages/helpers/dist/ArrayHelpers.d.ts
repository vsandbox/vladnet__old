export declare namespace ArrayHelpers {
    const findIndex: <T = any>(arr: T[], filter: (value: T, index: number) => boolean) => number;
    const a = 12;
    const splice: (arr: any[], index: number, value?: any) => any[];
}
